# Required: your OpenAI API key (https://platform.openai.com/api-keys)
OPENAI_API_KEY=

# Optional: override the vision model used (defaults to gpt-4o)
OPENAI_MODEL=gpt-4o

# Optional: port the server listens on (defaults to 3000)
PORT=3000
